# Filminex Web

The Sprint 0 frontend foundation is a React, TypeScript, and Vite application shell.
It demonstrates workspace context, capability-driven navigation, hierarchy selection,
and a consistent context inspector without implementing finished filmmaking workflows.

```shell
npm ci
npm run dev
npm test
npm run build
```

The shell uses deterministic local data until versioned backend API conventions and
authentication are established.
