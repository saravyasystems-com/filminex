spring:
  application:
    name: filminex-api

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

info:
  application:
    name: Filminex API
    description: The backend foundation for the Filmmaking Workspace
