package com.saravyasystems.filminex.ai.api;

/** Provider-neutral categories; filmmaking-specific schemas remain in their owning modules. */
public enum AiTaskType {
    TEXT_GENERATION,
    STRUCTURED_PROPOSAL,
    IMAGE_ANALYSIS,
    IMAGE_GENERATION
}
